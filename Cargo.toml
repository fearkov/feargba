[package]
name = "fear-gba"
version = "0.2.0"
edition = "2021"
description = "A Game Boy Advance emulator in Rust, inspired by gdkGBA"
license = "MIT"

[dependencies]
minifb = "0.27"
cpal = "0.15"

[profile.dev]
opt-level = 2

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
